import { tv } from "@nextui-org/react";

export const svg = tv({
  base: "", // estilos base do SVG
  variants: {
    // adicione variantes se necessário
  }
});

export const path = tv({
  base: "", // estilos base do Path
  variants: {
    // adicione variantes se necessário
  }
});